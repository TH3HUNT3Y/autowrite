[package]
name = "dripwriter"
version = "0.1.0"
edition = "2021"
description = "A local, OS-level text dripper for Windows"
license = "MIT"

[dependencies]

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true